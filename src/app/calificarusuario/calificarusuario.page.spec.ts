import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CalificarusuarioPage } from './calificarusuario.page';

describe('CalificarusuarioPage', () => {
  let component: CalificarusuarioPage;
  let fixture: ComponentFixture<CalificarusuarioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CalificarusuarioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
